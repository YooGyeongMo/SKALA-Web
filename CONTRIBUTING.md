# 기여 안내

이 저장소는 SKALA 실습 과정을 기록한 개인 프로젝트입니다. 소스는 공개되어 있지만
오픈소스 프로젝트처럼 자유롭게 포크해서 PR을 보내는 방식으로 운영하지 않습니다.

## 기여 전에 꼭 해주세요

1. **먼저 이슈로 제안해 주세요.** 버그 제보, 개선 제안 모두 이슈로 남겨 주시면
   검토 후 진행 여부를 답변드립니다.
2. **작업은 승인 후에 시작해 주세요.** 협의 없이 보낸 Pull Request는 내용과 무관하게
   닫힐 수 있습니다.
3. 급한 연락은 ykm7003@gmail.com 으로 주셔도 됩니다.

## 승인 후 작업 규칙

이 저장소의 커밋 이력과 같은 방식을 따라 주세요.

- **흐름**: 이슈 생성 → `main` 최신화 → 브랜치 생성 → 작업 단위 커밋 → PR → 머지
- **브랜치 이름**: `feat/`, `fix/`, `design/`, `docs/` 프리픽스 + 짧은 영문 설명
  (예: `fix/toast-default`)
- **커밋 메시지**: 한국어 한 줄, 끝에 이슈 번호 (예: `도시 목록 토스트 기본 동작 통일 (#114)`)
- **PR 본문**: 개요와 작업 내용을 한국어로, `Closes #이슈번호`로 이슈와 연결

## 코드 스타일

- Vue 3 Composition API(`script setup`)와 scoped 스타일을 사용합니다.
- 색상, 간격, 폰트는 `src/assets/styles/tokens.css`의 토큰을 사용합니다.
  하드코딩한 색상 값은 데이터가 색을 갖는 경우(기온 구간, 국기색)에만 허용합니다.
- 화면 문구는 자연스러운 한국어 문장으로 씁니다. 가운뎃점과 대시는 쓰지 않습니다.
- 로딩 표시는 기다림이 실재할 때만 보여줍니다. 캐시가 유효한 재방문에는
  스켈레톤을 띄우지 않는 기존 원칙을 지켜 주세요.

## 확인 절차

- 푸시 전에 `npm run build`가 통과하는지 확인해 주세요.
- API 키는 절대 커밋하지 마세요. 키는 `.env`(커밋 안 됨)에만 두고,
  견본은 `.env.example`을 사용합니다.
